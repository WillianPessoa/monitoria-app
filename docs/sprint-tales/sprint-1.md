# Sprint Tales — Sprint 1

**Sprint:** 1 — Perfis e Autenticação  
**Período:** 07/05/2026 – 18/05/2026  
**QM:** Willian Gomes Pessoa

---

| ID | Prioridade | Solução adotada | Quem resolveu | Alterado depois? | Tempo suficiente? | Arquivos alterados | Impacto em qual artefato |
|---|---|---|---|---|---|---|---|
| TT02 | must-have | Schema SQL com 4 tabelas: `usuarios`, `password_reset_tokens`, `disciplinas`, `monitorias`. Uso de `IF NOT EXISTS` para idempotência. FKs e constraints de unicidade declaradas. | João Pedro | Não | Sim | `backend/db/schema.sql` | Banco de dados — estrutura completa da Sprint 1 |
| TT03 | must-have | MySQL containerizado via `docker-sql.yaml` (MySQL 8.0, health check, volume persistente, schema aplicado automaticamente via `docker-entrypoint-initdb.d`). `.env.example` com todas as variáveis necessárias. | João Pedro | Não | Sim | `docker-sql.yaml`, `backend/.env.example` | Ambiente de desenvolvimento local do time |
| TT04 | must-have | App Flask com padrão factory (`create_app()`). Blueprints por domínio: `auth`, `usuarios`, `disciplinas`, `agenda`, `registros`, `relatorios`. Templates e estáticos em `frontend/` separado do `backend/`. | João Pedro | Sim (TT06 adicionou `Procfile` e `gunicorn`) | Sim | `backend/app.py`, `backend/config.py`, `frontend/` (estrutura completa) | Arquitetura — base de todos os módulos futuros |
| TT05 | must-have | Pool de conexões MySQL via `mysql-connector-python`. `init_db()` inicializa o pool na subida da app. `get_connection()` retorna conexão do pool. Falha de conexão é logada sem derrubar o app. | João Pedro | Sim (TT06 adicionou auto-apply do schema) | Sim | `backend/db/connection.py` | Camada de persistência — usada por todos os repositórios |
| TT06 | must-have | Deploy no Railway com `Procfile` (gunicorn) e `railpack.json` (declara `provider: python`). `config.py` atualizado para aceitar variáveis no formato do Railway (`MYSQLHOST` etc) como fallback. `connection.py` atualizado para aplicar o schema automaticamente na inicialização (idempotente via `IF NOT EXISTS`). Schema aplicado manualmente via script Python na primeira subida. Admin inicial criado via script direto no banco. | Willian | Sim (6 commits — múltiplas iterações para resolver detecção de linguagem e variáveis de ambiente) | Não | `Procfile`, `railpack.json`, `requirements.txt` (raiz), `backend/requirements.txt`, `backend/config.py`, `backend/db/connection.py` | Deploy — app acessível publicamente em https://web-production-1f724.up.railway.app |
| US01 | must-have | Blueprint `usuarios` com padrão service/repository. POST `/usuarios/` cria usuário com senha temporária gerada automaticamente. Status inicial `PENDENTE`. Queries parametrizadas (ADR-0004). Validação de email único. | João Pedro | Não | Sim | `backend/usuarios/routes.py`, `service.py`, `repository.py`, `frontend/templates/usuarios/index.html` | Tela de gestão de usuários (admin), tabela `usuarios` |
| US02 | must-have | Autenticação via Flask sessions (ADR-0003). Hash de senha com `werkzeug`. Email normalizado para lowercase. Redirecionamento pós-login baseado no papel. Mensagens de erro genéricas para não vazar existência de email. | João Pedro | Não | Sim | `backend/auth/routes.py`, `service.py`, `repository.py`, `decorators.py`, `frontend/templates/auth/login.html` | Tela de login, mecanismo de sessão |
| US03 | should-have | GET/POST `/usuarios/my-profile`. Campos editáveis: `contato` e `disponibilidade`. Decorador `@login_required`. Tela básica — expandida nas próximas sprints com agenda e monitorias. | João Pedro | Não | Sim | `backend/usuarios/routes.py`, `repository.py`, `frontend/templates/usuarios/my_profile.html` | Tela de perfil do monitor, tabela `usuarios` |
| US04 | should-have | POST `/usuarios/<id>/desativar` marca usuário como `INATIVO`. Validação: admin não pode desativar a si mesmo (previne lock-out). Usuário inativo bloqueado no login. | João Pedro | Não | Sim | `backend/usuarios/routes.py`, `service.py` | Tela de gestão de usuários (admin), tabela `usuarios` |
